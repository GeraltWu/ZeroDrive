# Data directory for file blobs (created if missing)
DATA_ROOT=./data/files

# SQLite database file path
SQLITE_PATH=./data/zerodrive.db

# Single admin (development: plain password comparison — replace with hash in production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# JWT (use a long random string in production)
JWT_SECRET=
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=525600

# CORS (dev frontend)
CORS_ORIGINS=http://localhost:5173

BOT_API_TOKEN=qqbotdesu
BOT_DOWNLOAD_EXPIRE_SECONDS=300